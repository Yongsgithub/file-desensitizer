[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "file-desensitizer"
version = "0.1.0"
description = "文件信息脱敏工具 — 自动识别并遮盖Word/PDF/压缩包中的姓名、手机号、身份证、住址、学号、户籍地址等敏感信息"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
authors = [
    {name = "CodeBuddy Skill"}
]
keywords = ["desensitization", "privacy", "PII", "redaction", "ocr", "MCP"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Security",
    "Topic :: Utilities",
]

dependencies = [
    "PyMuPDF>=1.23.0",
    "python-docx>=1.0.0",
    "tkinterdnd2>=0.3",
    "mcp>=1.0.0",
]

[project.optional-dependencies]
mcp = ["mcp>=1.0.0"]

[project.scripts]
file-desensitizer = "file_desensitizer.main:main"
file-desensitizer-gui = "file_desensitizer.gui:main"

[project.entry-points."mcp.servers"]
file-desensitizer-mcp = "file_desensitizer.server:mcp"

[tool.setuptools.packages.find]
where = ["src"]
